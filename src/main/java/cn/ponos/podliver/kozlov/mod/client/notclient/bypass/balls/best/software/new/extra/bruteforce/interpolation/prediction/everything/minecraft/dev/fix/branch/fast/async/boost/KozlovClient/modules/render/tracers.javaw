for (int i = 0; i < 100; i++) {
	// finding kozlov, so your enemies can't run away from you.
	// also tries to set his motion to 0 by using Bypass interpolation prediction everything module.
	// Credits: KozlovHacking.

	System.out.println("%player% IS NEARBY!!!").replace("%player%", "kozlov")); 
	kozlov.setMotion.x(0.0D);
	kozlov.setMotion.z(0.0D);
}
